type Comment {
  _id: ID!
  name: String!
  createdTime: String!
  email: String!
  content: String!
  refTo: Comment
  postId: ID
  title: String
}

type Comments {
  comments: [Comment]
  total: Int
  page: Int
}

type Messages {
  comments: [Comment]
  total: Int
  page: Int
}