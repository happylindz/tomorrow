type Query {
  post(url: String): Post
  posts(time: String, page: Int, size: Int): Posts
  info: Info
  projects(page: Int, size: Int): Projects
  comments(page: Int!, size: Int): Comments
  message(page: Int!, size: Int): Message
}
